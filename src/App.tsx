import { css } from '@styled-system/css';

const App = () => (
  <div
    className={css({
      color: 'mustard',
      fontWeight: 'bold',
      zIndex: 'overlay',
    })}
  >
    1234
  </div>
);

export default App;
